# Cinderella — environment configuration
# Copy to `.env` and fill in real values. `.env` is git-ignored; never commit it.
# Use placeholders only in this file — never real IPs, secrets, or member data.

# --- SimpleX chat core (embedded in-process via the simplex-chat native SDK) ---
# There is NO separate daemon and NO network port: the bot embeds the Haskell
# chat core. The SimpleX core owns a local SQLite DB and a files folder, both on
# the same host as the bot.

# Display name of the bot's own SimpleX profile.
BOT_DISPLAY_NAME=Cinderella

# File prefix for the embedded SimpleX core DB (SQLite). The core creates
# <prefix>_chat.db and <prefix>_agent.db. This DB holds the bot's SimpleX
# identity/state — keep it under state/ (git-ignored) and lock down file perms.
SIMPLEX_DB_PREFIX=./state/simplex/cinderella

# Files folder the SimpleX core writes received (XFTP) files into, before
# Cinderella moves them into MEDIA_ROOT. Must be on the same host as the bot.
SIMPLEX_FILES_FOLDER=./state/files

# Optional: name of the group Cinderella archives. Used to scope/verify capture.
# Leave empty to capture from every group the bot is a member of.
GROUP_NAME=cinderella-test

# How the bot is hosted (CCB-S4-021, D-125; the switch went in D-155): every enabled
# bot runs on the multi-profile runtime, and nothing is sent until the SimpleX core
# has finished subscribing (a restart leaves the bots receiving but mute for about
# ten seconds, deliberately: a send into the warm-up takes ~10s to reach its first
# receiver against ~150ms on a settled core). There is no hosting switch any more:
# BOT_RUNTIME_HOSTING and the pre-runtime path it selected were removed, because a
# lever that silently reduced the deployment to one bot was not a rollback, it was a
# defect with a handle. Rolling back a bad deploy is a checkout of the previous
# revision (see deploy/RUNBOOK.md).

# --- Cinderella media store ---
# Cinderella's own media tree (git-ignored). Captured files are moved here and
# the path is recorded in the database. Keep separate from SIMPLEX_FILES_FOLDER.
MEDIA_ROOT=./media

# Where quarantined media is moved (CCB-S3-013). A hash match or an operator
# escalation relocates the files here, physically OUT of MEDIA_ROOT, because the
# admin console must not be able to serve them by any path. It MUST NOT be inside
# MEDIA_ROOT; the app refuses to start if it is. Defaults to a sibling of
# MEDIA_ROOT, so leaving this unset is fine.
# QUARANTINE_ROOT=./quarantine

# Encryption of ORIGINAL media at rest (CCB-S3-012). 32+ characters, random.
# Generate with:  openssl rand -base64 48
#
# READ THIS BEFORE SETTING IT. There is no key history and no re-wrap on read:
# rotating or losing MEDIA_SECRET makes every already-encrypted original
# permanently unreadable. deploy/backup.sh DOES capture this key: it archives the
# whole env file, encrypted under the passphrase in BACKUP_PASSPHRASE_FILE, which
# lives OUTSIDE the env file precisely so the two never travel together. What must
# be backed up separately is that passphrase; lose it and every backup, media and
# key alike, restores to unreadable bytes. (This note used to claim backup.sh does
# not capture /etc/cinderella, contradicting the RUNBOOK; the RUNBOOK was right.)
#
# Unset means encryption is OFF and originals are stored as-is, which is a valid
# choice and is reported as such in the admin console. Setting it encrypts NEW
# originals; run `npm run encrypt-media` to backfill the existing ones.
# MEDIA_SECRET=

# --- PostgreSQL (the archive DB — separate from the SimpleX core's SQLite DB) ---
# Canonical connection string. Do not hardcode credentials in source.
DATABASE_URL=postgres://cinderella:CHANGEME@127.0.0.1:5432/cinderella

# --- Admin console (A3) ---
# The admin server binds to 127.0.0.1:ADMIN_PORT — nginx terminates TLS and
# proxies to it. Never expose this port directly.
ADMIN_PORT=8787

# Single operator account. Generate the Argon2id hash with:
#   npm run hash-password
# Quote the hash — it contains $ characters. Never store the plain password.
ADMIN_USERNAME=operator
ADMIN_PASSWORD_HASH='$argon2id$CHANGEME'

# Secret for signing session cookies. At least 32 random characters, e.g.:
#   openssl rand -hex 32
SESSION_SECRET=CHANGEME_at_least_32_random_characters_here

# Public origin of the admin console. Also the WebAuthn Relying Party: the RP ID
# (registrable domain) and expected origin derive from this, so it must be the
# real https:// admin hostname in production (WebAuthn needs a secure context and
# a domain-based RP ID — not a bare IP or plain HTTP).
PUBLIC_ORIGIN=https://cinderella.example.org

# Optional WebAuthn overrides (normally derived from PUBLIC_ORIGIN):
#   WEBAUTHN_RP_ID=cinderella.example.org
#   WEBAUTHN_ORIGIN=https://cinderella.example.org
#   WEBAUTHN_RP_NAME=Cinderella Admin

# --- Private local AI intent resolver ---
# Disabled by default. When enabled, Cinderella sends only the addressed
# instruction to a private Ollama endpoint for classification. Public endpoints
# are rejected by configuration validation.
LOCAL_AI_ENABLED=false

# Use loopback when Cinderella and Ollama run on the same machine. For a private
# WireGuard deployment, replace this with the GPU host's private tunnel address.
LOCAL_AI_BASE_URL=http://10.8.0.4:11434

# Local Ollama model and per-request timeout.
LOCAL_AI_MODEL=qwen3.5:9b
LOCAL_AI_TIMEOUT_MS=15000

# --- Runtime ---
# Log verbosity: error | warn | info | debug
LOG_LEVEL=info

# Where the music library lives (CCB-S5-044). Optional; defaults to a SIBLING of
# MEDIA_ROOT called music-library/, and is REFUSED inside MEDIA_ROOT (the
# destruction sweeper walks that tree and would sweep id-shaped track files).
# MUSIC_ROOT=./music-library
